# memlog — Architecture Spine: Sagar's Portfolio
scope: Full-stack developer portfolio — Next.js frontend + Express API + MongoDB, monorepo. Feature altitude (keeps epics).
purpose: build-substrate
altitude: feature

- [2026-07-06] event: Architecture run initialized. Workspace: _bmad-output/planning-artifacts/architecture/architecture-sagars-portfolio-2026-07-06/.
- [2026-07-06] override: python3/python/uv unavailable on this Windows machine — memlog maintained by hand in append-only script format; resolve_customization.py and lint_spine.py bypassed, customize.toml defaults read directly.
- [2026-07-06] constraint: MANDATED stack (NFR-5, non-negotiable) — Frontend: Next.js App Router + TypeScript + ShadCN + React Query + RHF/Zod; stretch libs Framer Motion + React Three Fiber. Backend: Express + Mongoose. DB: MongoDB Atlas. This is a first-class requirement (skills demonstration), not an architect's free choice.
- [2026-07-06] constraint: Inherited PRD decisions D-1..D-7 (provisional, Sagar may override taste items D-1/D-7): threshold-gated visitor counter; monorepo (frontend/ + backend/); Render for API host; store-only contact messages v1; env-var + session-cookie admin auth; small/fast LLM model; cursor-reactive particle 3D hero.
- [2026-07-06] constraint: Hard guardrail NFR-1 — Lighthouse >= 90 all categories; 3D (R3F) lazy-loaded + code-split after first paint; images optimized/lazy; prefers-reduced-motion honored.
- [2026-07-06] constraint: NFR-4 security — all secrets (MONGODB_URI, LLM_API_KEY, admin creds) server-side only, never in client bundle or repo; AI route rate-limited; admin authenticated; all inbound data Zod-validated.
- [2026-07-06] version: Web-verified current versions (July 2026): Node.js >=22 LTS; Next.js 16.2.x (App Router, React 19.2, Turbopack default); React 19.2; TypeScript 5.x; Tailwind CSS 4.x; shadcn/ui current CLI (React 19/Tailwind 4); @tanstack/react-query 5.101.x; react-hook-form 7.81.x; zod 4.4.x (+ @hookform/resolvers); Motion 12.42.x (pkg renamed framer-motion -> motion, import motion/react); three + @react-three/fiber 9.6.x (bundles reconciler for React 19.2) + @react-three/drei; Express 5.2.x (async error propagation built in); Mongoose 9.7.x.
- [2026-07-06] version: Stack is coherent on React 19.2 — R3F 9, Motion 12, TanStack Query 5 all confirm React 19 support. No version conflict binding the mandated + stretch libs together.
- [2026-07-06] decision: AD-1 [ADOPTED] Paradigm = layered monorepo. Frontend: Next.js App Router segmented by route groups (site)/(admin), colocated components. Backend: layered Express (routes -> controllers -> services -> Mongoose models). Binds all. Prevents ad-hoc structure across independently built units.
- [2026-07-06] decision: AD-2 Backend is the SOLE owner/writer of MongoDB; frontend never connects to Mongo directly — all reads/writes go through the Express REST API. Prevents two writers and schema drift.
- [2026-07-06] decision: AD-3 API response envelope fixed: success {data}, error {error:{message,code}}, meaningful HTTP status. Prevents each endpoint inventing its own shape.
- [2026-07-06] decision: AD-4 Domain entities + ownership: Project, Experience, Message, View(counter). Project carries a unique human slug used as the public route param /projects/[slug]; Mongo _id is internal only. Prevents route/id ambiguity and duplicate identity strategies.
- [2026-07-06] decision: AD-5 Read vs write flow: read data (projects/experience) server-rendered via Next SSG/ISR (generateStaticParams, revalidate on admin mutation) for SEO/Lighthouse; interactive data (contact submit, admin CRUD, weather, AI draft) via React Query in client components. Prevents inconsistent fetching + SEO regressions. (Resolves the data-fetching + rendering forks toward server-read/RQ-write, static projects.)
- [2026-07-06] decision: AD-6 Validation boundary: every inbound API payload Zod-validated at the route boundary before service/model; the SAME zod schemas (in shared/) back the RHF forms. Prevents unvalidated writes and frontend/backend rule drift.
- [2026-07-06] decision: AD-7 [ADOPTED] Secrets server-side only (git-ignored env, host dashboards); NEXT_PUBLIC_* only for non-secret frontend config (API base URL). Mongo URI, LLM key, admin creds live on backend host only. Prevents secret leakage into client bundle/repo.
- [2026-07-06] decision: AD-8 Admin auth = backend-issued signed JWT in httpOnly+Secure+SameSite cookie on login; admin API routes gated by auth middleware; CORS with credentials for the single frontend origin. Chosen over server-side session store for clean cross-domain (Vercel<->Render) auth. Amends/hardens D-5.
- [2026-07-06] decision: AD-9 AI LLM call lives ONLY on Express backend (POST /api/ai/draft); key server-side; per-IP rate-limit middleware + token cap. Frontend never calls the LLM. Resolves AI-route fork toward Express (keeps all backend logic + middleware demo in one service). Prevents key exposure + cost blowout.
- [2026-07-06] decision: AD-10 Performance budget: R3F hero + heavy client libs dynamically imported (code-split), mounted after first paint; static fallback under prefers-reduced-motion and on low-power/coarse-pointer devices; images via next/image WebP, lazy below hero; Lighthouse >=90 is a merge gate. Ties NFR-1 into an enforceable rule.
- [2026-07-06] decision: AD-11 [ADOPTED] Visitor counter: view increments via backend endpoint; public display threshold-gated (hidden until floor N passed); raw counts admin-only until then. D-1 (taste, Sagar may override). Prevents low early counts becoming a negative trust signal.
- [2026-07-06] decision: AD-12 Shared TypeScript domain contract: canonical Project/Experience/Message types + API envelope + category enum defined ONCE in shared/ and consumed by both frontend and backend. Prevents type drift across the HTTP boundary. Sharing mechanism (npm workspace vs tsconfig-path-mapped shared/ folder) deferred to scaffold; the contract is the invariant.
- [2026-07-06] direction: Hosting/operational envelope — frontend Vercel (Git-connected), backend Render web service, DB MongoDB Atlas M0; local .env + production host-dashboard envs. GitHub Pages excluded (can't host Express/SSR).
- [2026-07-06] decision: Dependency direction — shared/ depends on nothing; backend depends on shared/; frontend depends on shared/ + the API over HTTP only. No frontend->backend source import, no backend->frontend dependency.
- [2026-07-06] event: Full spine drafted (ARCHITECTURE-SPINE.md) — 12 ADs, conventions table, verified stack, container+dependency+ERD diagrams, source tree, operational envelope, capability map, deferred list.
- [2026-07-06] version: Reviewer gate lens-1 closed remaining version gaps — shadcn/ui + Tailwind v4 + Next 16 + React 19 verified compatible; Node 24 is Active LTS (July 2026). Stack pin updated Node ">=22 LTS" -> "24 LTS (Active)".
- [2026-07-06] decision: AD-4 amended in place (gate lens-2 HIGH) — backend now enforces slug uniqueness (unique index; reject/auto-derive on collision). Closes slug-collision divergence where two units both treat slug as identity yet clash.
- [2026-07-06] decision: AD-5 amended in place (gate lens-2 HIGH) — named the freshness owner: backend triggers Next.js on-demand revalidation after a successful mutation (webhook/tag) + time-based fallback, because static pages (Vercel) and writes (Render) are on separate hosts. Added note: weather widget client-fetch is valid only for a KEYLESS API; keyed provider must be backend-proxied (AD-7).
- [2026-07-06] decision: Deferred shared-types note strengthened (gate lens-2 MEDIUM) — shared/ must be plain TS, no runtime deps, compile under both Next and Express/tsc builds (dual ESM/CJS if formats differ). Prevents the shared contract failing to compile on one side.
- [2026-07-06] event: Reviewer gate run inline (subagents not spawned per environment guidance; self-review noted as less independent). Verdict PASS with fixes applied. Full record: reviews/review-gate.md. Low items left to code: view double-count dedupe, CORS preview-origin handling.
- [2026-07-06] event: spine finalized.
