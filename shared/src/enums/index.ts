// Canonical domain enums shared across the stack.

/** Whether an Experience entry is professional work or education. */
export enum ExperienceKind {
  Work = "work",
  Education = "education",
}

/** Project category for the projects showcase. */
export enum ProjectCategory {
  FullStack = "full-stack",
  Frontend = "frontend",
  Backend = "backend",
  AI = "ai",
  RealTime = "real-time",
}
